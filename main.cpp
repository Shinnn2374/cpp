#include <iostream>

using namespace std;

int main(){
    int n, len;
    cout << "Введите число - " ;
    cin >> n;
    len = n;
    int ar[len];
    while (n <= len){
        if (n%2){
            ar
        }
        n++;
    }
    return 0;
}